# P0 手工回归清单（青砚一期）

> 用途：合并前 / 发布前快速跑一遍核心路径。  
> **依赖环境变量**的用例已标注 `[ENV]`。

## 环境与构建

| 步骤 | 预期 | 实际 |
|------|------|------|
| `npm run qa`（lint + prisma generate + next build） | 退出码 0；**ESLint 无 error、无 warning** | |
| `npm run build`（含 migrate deploy）`[ENV]` | 生产构建成功；数据库可连 | |

## 认证

| 步骤 | 预期 | 实际 |
|------|------|------|
| 未登录访问 `/tasks` | 重定向到 `/login` | |
| 注册新用户 → 登录 | 进入工作台 `/` | |
| 登录后访问受保护 API（如 `/api/me` 经页面间接） | 非 401 | |
| 退出登录 | 回到登录页，再访问主站需登录 | |

## 工作台与导航

| 步骤 | 预期 | 实际 |
|------|------|------|
| 打开 `/` | 统计与区块加载；无白屏 | |
| 侧栏「使用说明」`/help` | 功能地图表可点链 | |
| 顶栏搜索输入关键字 `⌘K` / `Ctrl+K` | 面板打开；`Esc` 关闭并清空 | |
| 顶栏提醒铃铛 | 面板打开；列表与「查看全部任务」可用 | |

## 任务 CRUD

| 步骤 | 预期 | 实际 |
|------|------|------|
| `/tasks` 新建任务 | 列表出现；骨架/空状态合理 | |
| 任务详情 `/tasks/[id]` 改状态/优先级 | 保存成功 | |
| 评论与活动区 | 可加载与发送（若实现） | |

## 组织与项目

| 步骤 | 预期 | 实际 |
|------|------|------|
| `/organizations` 创建组织 | 列表出现；说明文案可见 | |
| `/projects` 新建项目（选组织） | 卡片出现；搜索可跳到项目详情 `[如已实现]` | |
| `/projects/[id]` 首屏 | 标题、组织链接、成员/环境区块 | |

## 收件箱与 AI 助手

| 步骤 | 预期 | 实际 |
|------|------|------|
| `/inbox` 输入一条事项并发送 | 流式或最终结果；与助手页文案区分清晰 | |
| `/assistant` 多轮对话 | 上下文延续；无 Key 时配置引导 | |
| `[ENV]` 未配置 `OPENAI_API_KEY` | 助手/收件箱友好提示，不白屏 | |

## Google 日历与设置

| 步骤 | 预期 | 实际 |
|------|------|------|
| `/settings` 查看连接状态 | 已连/未连状态清晰 | |
| `[ENV]` 点击连接 Google（已配 OAuth） | 回调成功或错误 reason 有中文说明 | |
| `[ENV]` 折叠配置说明 | 长文案可展开阅读 | |

## 工艺单（若启用）

| 步骤 | 预期 | 实际 |
|------|------|------|
| `/blinds-orders` 列表与新建 | 可打开 | |

## 发布前 Checklist（Vercel）

- [ ] `DATABASE_URL` / `DIRECT_URL`（Neon 建议双 URL）
- [ ] `JWT_SECRET`
- [ ] `OPENAI_*` 或兼容供应商 Key/BaseURL/Model
- [ ] Google OAuth：`GOOGLE_CLIENT_ID`、`GOOGLE_CLIENT_SECRET`、`GOOGLE_REDIRECT_URI` 与控制台一致 `[ENV]`
- [ ] `npx prisma migrate deploy` 已在目标库执行

---

**自动化**：仓库提供 `npm run qa`（不含 migrate，避免本地锁问题）；完整生产构建用 `npm run build`。
